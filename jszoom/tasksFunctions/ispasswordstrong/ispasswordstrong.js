//Задача8
//Создайте функцию isPasswordStrong(password), которая проверяет,
// что пароль состоит минимум из 6 символов и содержит хотя бы одну цифру.

function isPasswordStrong(password) {
    if (password.length<6)  {
        return "ПАРОЛЬ ДОЛЖЕН СОДЕРЖАТЬ 6 ИЛИ БОЛЕЕ СИМВОЛОВ"
    }
    //Метод indexOf() сравнивает искомый элемент searchElement с элементами в массиве, используя строгое сравнение (en-US) (тот же метод используется оператором ===, тройное равно).
    else
    {

        for (let countNumber=0, countNumber>=password.length, countNumber++){

            }
        }
        }

module.exports={isPasswordStrong}